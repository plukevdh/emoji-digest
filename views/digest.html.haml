%h1 Digest Emoji
- if defined? date
  %h3
    for week of
    = date.strftime("%m/%d/%Y")

- emoji.each do |group, collection|
  %h3= group.capitalize
  - if collection.empty?
    Nothing changed this week.
  - else
    - collection.each_slice(5) do |group|
      .row
        - group.each do |name, url|
          .emoji-listing{ class: name }
            %img.emoji{ src: url, width: '18', height: '18' }
            %b.name= name